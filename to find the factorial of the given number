#include <stdio.h>
int main() {
    int i,n,fact=1;
    printf("enter an number: ");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        fact*=i; 
    }
    printf("fact = %d ",fact);
    return 0;
}
