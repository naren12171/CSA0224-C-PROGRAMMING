#include <stdio.h>

int main() {
	int n,i;
	float num,sum=0,average;
	scanf("%d",&n);
	for(i=1;i<=n;i++){
	    scanf("%f",&num);
	sum=sum+num;
	}
average=sum/n;
printf("average=%.2f\n",average);
return 0;
}
