#include <stdio.h>

int main() 
{ 
  int height;
  scanf("%d",&height);
  if(height<147) {
      printf("%d=dwarf");
  }
  else if(height>148 && height<155){
      printf("%d=average");
  }
else if (height>156 && height<175){
    printf("%d=tall");
}
return 0;
}
