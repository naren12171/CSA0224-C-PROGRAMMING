#include<stdio.h>
#include<math.h>
int main()
{
    float a,b,c;
    float r1,r2;
    r1=-b+ sqrt(b*b-4*a*c)/(2*a);
    r2=-b- sqrt(b*b-4*a*c)/(2*a);
    scanf("%f%f%f",&a,&b,&c);
    printf("Root1=%.2f\n",r1);
    printf("Root2=%.2f\n",r2);
    return 0;
}
